% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/write_outputs.R
\name{write_outputs}
\alias{write_outputs}
\alias{write_outputs.x13_result}
\title{Write an x13_result's contents to disk}
\usage{
write_outputs(x, path, ...)

\method{write_outputs}{x13_result}(x, path, basename = "series", overwrite = FALSE, ...)
}
\arguments{
\item{x}{An \link{x13_result} object.}

\item{path}{Directory to write into. Created if it does not exist.}

\item{...}{Passed on to methods; currently unused.}

\item{basename}{File basename (without extension) for the written files.
Defaults to \code{"series"}.}

\item{overwrite}{Logical; overwrite existing files at \code{path}? Default
\code{FALSE}, which errors if any target file already exists.}
}
\value{
\code{path}, invisibly.
}
\description{
\code{seasonal_adjust()} never writes files as a side effect -- everything it
computes lives on the returned \link{x13_result} object. \code{write_outputs()} is
the single, explicit, opt-in way to persist that object's contents,
mirroring the file layout the Census X-13ARIMA-SEATS program itself
writes (one file per save table, plus a \code{.udg} diagnostics file and a
\code{.fct} forecast file), so output from this package can be diffed against
the Fortran oracle's output if desired.
}
