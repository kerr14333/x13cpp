% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/x13_result.R
\name{x13_result}
\alias{x13_result}
\alias{new_x13_result}
\alias{udg}
\alias{fct}
\alias{save_table}
\alias{seasadj}
\alias{trend}
\alias{irregular}
\title{X13cpp result object}
\usage{
new_x13_result(
  call = sys.call(-1),
  series = NULL,
  spec = list(),
  udg = list(),
  fct = NULL,
  save = list(),
  status = c("stub", "ok")
)

udg(x, ...)

fct(x, ...)

save_table(x, table, ...)

seasadj(x, ...)

trend(x, ...)

irregular(x, ...)
}
\arguments{
\item{call}{The matched call that produced the result (see "Structure").}

\item{series}{The original input series, a \code{ts} object (see "Structure").}

\item{spec}{The resolved spec list for the run (see "Structure").}

\item{udg}{A named list of scalar diagnostics (see "Structure").}

\item{fct}{A forecast table, or \code{NULL} (see "Structure").}

\item{save}{A named list of save tables (see "Structure").}

\item{status}{Either \code{"stub"} or \code{"ok"} (see "Structure").}

\item{x}{An \code{x13_result} object.}

\item{...}{Passed on to methods; currently unused.}

\item{table}{A save-table code, e.g. \code{"d11"} for the seasonally adjusted
series, \code{"d12"} for the trend, \code{"d13"} for the irregular. See the X-13
documentation for the full table code list.}
}
\description{
\code{seasonal_adjust()} returns an object of S3 class \code{x13_result}. It is a
plain list with a fixed set of named elements, described below, plus the
accessor functions on this page for reading them out. Nothing about a
result object writes to disk on its own -- see \code{\link[=write_outputs]{write_outputs()}}.
}
\section{Functions}{
\itemize{
\item \code{seasadj()}: Convenience wrapper for the seasonally adjusted
series (save table \code{"d11"}).

\item \code{trend()}: Convenience wrapper for the trend component (save
table \code{"d12"}).

\item \code{irregular()}: Convenience wrapper for the irregular component
(save table \code{"d13"}).

}}
\section{Structure}{

\describe{
\item{\code{call}}{The matched call that produced the result.}
\item{\code{series}}{The original input series (a \code{ts} object).}
\item{\code{spec}}{The resolved spec list used for the run (mirrors an X-13
\code{.spc} file structure).}
\item{\code{udg}}{A named list of scalar diagnostics, mirroring the \code{.udg}
(user diagnostics) file X-13 writes -- e.g. \code{f3.m01} (M1 quality
statistic), \code{nobs}, \code{aicc}, and so on. Empty until the core is wired
in.}
\item{\code{fct}}{A forecast table: a \code{ts}/\code{data.frame}-like object with the
forecast values, standard errors, and confidence bounds. \code{NULL}
until the core is wired in or if the run did not request forecasts.}
\item{\code{save}}{A named list of "save tables", keyed by the X-13 table
code (\code{"d10"}, \code{"d11"}, \code{"d12"}, \code{"d13"}, \code{"s10"}, \code{"s11"}, ...),
each a \code{ts} object. Empty until the core is wired in.}
\item{\code{status}}{One of \code{"stub"} (core not yet wired in) or \code{"ok"}.}
}
}

