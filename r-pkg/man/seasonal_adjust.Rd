% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/seasonal_adjust.R
\name{seasonal_adjust}
\alias{seasonal_adjust}
\title{Run a seasonal adjustment}
\usage{
seasonal_adjust(
  x,
  transform = c("auto", "log", "none"),
  arima = NULL,
  x11 = FALSE,
  seats = FALSE,
  forecast_periods = 0L,
  ...
)
}
\arguments{
\item{x}{A \code{ts} object (or a numeric vector plus \code{start}/\code{frequency}, see
\code{stats::ts()}) giving the series to seasonally adjust.}

\item{transform}{Transformation to apply before modeling: \code{"auto"}
(choose automatically), \code{"log"}, or \code{"none"}.}

\item{arima}{ARIMA order specification. Not yet processed; reserved for
the same syntax as an X-13 \verb{arima\{ model = (p d q)(P D Q) \}} spec, e.g.
\code{"(0 1 1)(0 1 1)"}.}

\item{x11}{Logical; if \code{TRUE}, run the X-11 decomposition. Mutually
exclusive with \code{seats}.}

\item{seats}{Logical; if \code{TRUE}, run the SEATS decomposition. Mutually
exclusive with \code{x11}.}

\item{forecast_periods}{Number of periods to forecast beyond the end of
the series (0 disables forecasting).}

\item{...}{Additional spec options, reserved for future use.}
}
\value{
An \link{x13_result} object. This function never writes to disk; call
\code{\link[=write_outputs]{write_outputs()}} on the result explicitly if you want files.
}
\description{
\code{seasonal_adjust()} is the main entry point of the package: it runs the
X-13ARIMA-SEATS pipeline (regARIMA modeling, X-11 or SEATS decomposition,
diagnostics) over an input series and returns an \link{x13_result} object.
}
\section{Status -- core not yet wired in}{

The C++ estimation engine this function calls into is still mid-port (see
the \code{x13new} repository: regARIMA estimation is implemented, but the X-11
and SEATS decomposition paths are not). This function currently always
\code{stop()}s with an explanatory message rather than returning incorrect
numbers.

TODO(core wiring): once \verb{core/} exposes a stable C ABI / Rcpp-callable
entry point for a full run (regARIMA + X-11 or SEATS + diagnostics),
replace the \code{stop()} below with the real call and populate \code{udg}, \code{fct},
and \code{save} on the returned \link{x13_result}. The rest of this function's
contract (argument names, the shape of the returned object, the "never
write files automatically" rule) is intended to be final already.
}

\examples{
\dontrun{
# Once the core is wired in:
fit <- seasonal_adjust(x13cpp::airline, transform = "log",
                        arima = "(0 1 1)(0 1 1)", x11 = TRUE)
seasadj(fit)
udg(fit)
}
}
